# Downloaded / built artifacts — never committed to the source branch
*.deb
/public/
/debs/
/.gnupg-tmp/
*.gpgsig
# generated by setup-signing.sh / rotate-signing-key.sh; CI serves it from gh-pages
pubkey.gpg

# Local-only files that are not part of the published repo
install_openbao-latest.sh
project-concept.md

# Editor / OS noise
*.swp
.DS_Store
